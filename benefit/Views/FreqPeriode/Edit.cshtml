@model benefit.Models.FreqPeriode

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FreqPeriode</legend>

        @Html.HiddenFor(model => model.FreqPeriodeID)

        <div class="editor-label">
            @Html.LabelFor(model => model.FreqPeriodeName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FreqPeriodeName)
            @Html.ValidationMessageFor(model => model.FreqPeriodeName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>