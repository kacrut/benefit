@model IEnumerable<benefit.Models.FreqPeriode>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.FreqPeriodeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.FreqPeriodeName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.FreqPeriodeID }) |
            @Html.ActionLink("Details", "Details", new { id=item.FreqPeriodeID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.FreqPeriodeID })
        </td>
    </tr>
}

</table>
