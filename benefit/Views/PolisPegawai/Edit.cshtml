@model benefit.Models.PolisPegawai

@{
    ViewBag.Title = "Edit";
}

<h2>Edit</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>PolisPegawai</legend>

        @Html.HiddenFor(model => model.PolisPegawaiID)

        <div class="editor-label">
            @Html.LabelFor(model => model.PolisPegawaiName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.PolisPegawaiName)
            @Html.ValidationMessageFor(model => model.PolisPegawaiName)
        </div>

        <p>
            <input type="submit" value="Save" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
