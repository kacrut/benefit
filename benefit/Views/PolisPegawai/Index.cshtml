@model IEnumerable<benefit.Models.PolisPegawai>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PolisPegawaiName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PolisPegawaiName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PolisPegawaiID }) |
            @Html.ActionLink("Details", "Details", new { id=item.PolisPegawaiID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PolisPegawaiID })
        </td>
    </tr>
}

</table>
