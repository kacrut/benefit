@model IEnumerable<benefit.Models.ManfaatItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.ManfaatItemName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.ManfaatItemName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ManfaatItemID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ManfaatItemID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ManfaatItemID })
        </td>
    </tr>
}

</table>
