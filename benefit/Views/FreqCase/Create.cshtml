@model benefit.Models.FreqCase

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>FreqCase</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.FreqCaseName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.FreqCaseName)
            @Html.ValidationMessageFor(model => model.FreqCaseName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>