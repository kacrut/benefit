@model IEnumerable<benefit.Models.CaraBayar>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CaraBayarName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.CaraBayarName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.CaraBayarID }) |
            @Html.ActionLink("Details", "Details", new { id=item.CaraBayarID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.CaraBayarID })
        </td>
    </tr>
}

</table>
