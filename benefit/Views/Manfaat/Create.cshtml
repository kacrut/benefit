@model benefit.Models.Manfaat

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>Manfaat</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.NamaManfaat)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.NamaManfaat)
            @Html.ValidationMessageFor(model => model.NamaManfaat)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
