@model IEnumerable<benefit.Models.Manfaat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table table-striped table-bordered">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.NamaManfaat)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.NamaManfaat)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ManfaatID }) |
            @Html.ActionLink("Details", "Details", new { id=item.ManfaatID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ManfaatID })
        </td>
    </tr>
}

</table>
