@model benefit.Models.ManfaatPISA

@{
    ViewBag.Title = "Create";
}

<h2>Create</h2>

@using (Html.BeginForm()) {
    @Html.AntiForgeryToken()
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>ManfaatPISA</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.ManfaatPISAName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.ManfaatPISAName)
            @Html.ValidationMessageFor(model => model.ManfaatPISAName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>